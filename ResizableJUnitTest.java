import static org.junit.Assert.*;

import org.junit.Test;

import junit.framework.TestCase;
import project2.ResizableArrayStack;

public class ResizableJUnitTest extends TestCase
{
   @Test
   //Various assorted inputs and expected outputs.
   public void testEvaluatePostfix()
   {
      String[] postfixExpressions = {""};
      String[] expectedResults = {""};
      for(int i = 0; i < postfixExpressions.length; i++)
      {
         //assertEquals(expectedResults[i], ResizableArrayStack.evaluatePostfix(postfixExpressions[i]);
      }
   }
}